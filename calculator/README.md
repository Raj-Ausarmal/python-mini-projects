# Python Calculator 🧮

A simple calculator made with Python.

The program takes two numbers and an operator from the user, then performs the selected calculation.

## How to Run

Open the terminal inside the calculator folder and run:

```bash
python 01_calculator.py
