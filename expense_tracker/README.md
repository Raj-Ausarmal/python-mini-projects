# Expense Tracker 💰

A simple Python program to keep track of daily expenses.

You can add an expense, view all the expenses you have entered, and check the total amount spent. The program keeps running until you choose to exit.

## How to Run

Open the terminal inside the `expense_tracker` folder and run:

```bash
python 01_expense_tracker.py
